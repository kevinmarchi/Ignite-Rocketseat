/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'avatars.githubusercontent.com',
            '192.168.1.102',
            'localhost'
        ],
    },
}

module.exports = nextConfig
